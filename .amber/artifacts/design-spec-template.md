# 架构设计文档 模板

## 元数据
```
version: 1.0
author: Agent C (Architect)
basedOnPRD: prd.md v{version}
status: [DRAFT | REVIEWING | APPROVED]
```

## 1. 系统架构概览

### 1.1 架构风格
{如: 分层架构 / 六边形架构 / 微服务 / 事件驱动}

### 1.2 架构图（文本描述）
```
┌─────────────────┐     ┌─────────────────┐
│   {模块A}       │────▶│   {模块B}       │
│  {描述}         │     │  {描述}         │
└────────┬────────┘     └─────────────────┘
         │
         ▼
┌─────────────────┐
│   {模块C}       │
│  {描述}         │
└─────────────────┘
```

### 1.3 需求-设计追溯
| PRD条目 | 设计元素 | 设计决策 |
|---------|---------|---------|
| PRD-1.1 | {模块/接口/组件} | {设计决策说明} |
| PRD-1.2 | {模块/接口/组件} | {设计决策说明} |
| PRD-NFR-1 | {设计如何满足} | {设计决策说明} |

## 2. 模块详细设计

### 2.1 {模块A}
**职责**: {模块的核心职责}
**依赖关系**: {依赖哪些模块，被哪些模块依赖}
**关键设计决策**:
- {决策1}：{选择理由}
- {决策2}：{选择理由}

**内部结构**:
```
{模块内部的组织方式，如子模块划分、类图关系}
```

### 2.2 {模块B}
...

## 3. 接口契约

### 3.1 {接口/服务名}
**签名**: `{函数签名或API端点}`
**输入**: `{参数类型及约束}`
**输出**: `{返回类型及约束}`
**前置条件**: `{调用前必须满足的条件}`
**后置条件**: `{调用后保证的状态}`
**错误处理**: `{可能的错误及错误码}`
**引用PRD**: `[PRD-{N}]`

### 3.2 {数据模型}
```
{model Name}:
  field1: type | 约束 | 示例值 | 说明
  field2: type | 约束 | 示例值 | 说明
```

## 4. 关键流程设计

### 4.1 {流程名称}
```
1. {步骤1} — 由{模块X}处理
2. {步骤2} — 由{模块Y}处理
3. {步骤3} — 由{模块Z}处理
```
**注意**: {流程中的关键风险或特殊处理}

## 5. 设计权衡记录 (Tradeoffs)

参考 `artifacts/design/tradeoffs.md`

| 决策点 | 方案A (选) | 方案B (弃) | 方案C (弃) | 选择理由 |
|--------|-----------|-----------|-----------|---------|
| {决策点描述} | {优点/缺点} | {优点/缺点} | {优点/缺点} | {选择理由} |
